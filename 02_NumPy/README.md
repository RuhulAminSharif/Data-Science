# NumPy

The journey into NumPy begins here! NumPy is a powerful library for numerical computing in Python, providing support for arrays, matrices, and a wide range of mathematical functions.

## NumPy Fundamentals

> - What is NumPy?
> - NumPy Arrays vs Python Sequences
> - Creating NumPy Arrays
>   > - From Lists and Tuples
>   > - Using NumPy Functions
>   >   > - np.arange()
>   >   > - np.zeros()
>   >   > - np.ones()
>   >   > - np.linspace()
>   >   > - np.random.random()
>   >   > - np.identity()
> - Array Attributes
>   > - np.ndim
>   > - np.shape
>   > - np.size
>   > - np.itemsize
>   > - np.dtype
> - Changing Data Types
>   > - astype() Method
> - Array Operations
>   > - Scalar Operations
>   >   > - Arithmetic Operations
>   >   > - Relational Operations
>   > - Vectorized Operations
> - Array Functions
>   > - np.random.random(), np.round()
>   > - np.max(), np.min(), np.sum(), np.prod()
>   > - np.mean(), np.median(), np.std(), np.var()
>   > - np.sin(), np.cos(), np.tan()
>   > - np.dot()
>   > - np.log(), np.exp()
>   > - np.round(), np.floor(), np.ceil()
> - Indexing and Slicing
>   > - 1D Array Indexing
>   > - 2D Array Indexing
>   > - 3D Array Indexing
>   > - Slicing Arrays
> - Iterating Over Arrays
>   > - Using for Loops
>   > - Using np.nditer()
> - Reshaping Arrays
>   > - reshape() Method
>   > - np.transpose()
>   > - np.ravel()
> - Stacking and Splitting Arrays
>   > - Stacking: np.vstack(), np.hstack()
>   > - Splitting: np.vsplit(), np.hsplit()
